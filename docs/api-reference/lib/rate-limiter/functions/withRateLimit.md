[**bc-migration v0.1.0**](../../../README.md)

***

[bc-migration](../../../README.md) / [lib/rate-limiter](../README.md) / withRateLimit

# Function: withRateLimit()

> **withRateLimit**\<`T`\>(`fn`, `priority`): `T`

Defined in: [src/lib/rate-limiter.ts:55](https://github.com/nino-chavez/bc-migration/blob/main/src/lib/rate-limiter.ts#L55)

Wrap an async function with rate limiting

## Type Parameters

### T

`T` *extends* (...`args`) => `Promise`\<`any`\>

## Parameters

### fn

`T`

### priority

`number` = `5`

## Returns

`T`
